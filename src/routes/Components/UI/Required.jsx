import styled from "styled-components";

const Required = styled.span`
  color: rgba(121, 121, 121, 0.879);
  font-style: italic;
  font-size: smaller;
  font-size: small;
  align-self: flex-end;
`;

export default Required;
